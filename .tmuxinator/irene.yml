# ~/.tmuxinator/moriarty.yml

name: irene
root: ~/Projects/appknox/irene
tmux_options: -f ~/.tmux.conf
pre_window: irene

windows:
  - main:
      layout: main-vertical
      panes:
        - serve: ember serve
        - command: git status
